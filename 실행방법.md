# BEES Home v0.9 실행 방법

## 왜 더블클릭으로 열리지 않는가

v0.9 는 ES 모듈로 분할되어 있다. 브라우저는 `file://` 에서 모듈 `import` 를
**CORS 정책으로 차단**한다. v0.8 까지는 단일 HTML 이라 더블클릭으로 열렸지만,
v0.9 부터는 로컬 서버로 열어야 한다. (규칙 §10 "ES 모듈 구조를 유지한다")

## 실행

이 폴더에서 아래 한 줄을 실행한 뒤 브라우저로 접속한다.

```
node serve.js
```

→ http://localhost:8790

로그인 : ID `admin` / PW `123`

## 파일 구조

```
index.html            <style> · 셸 DOM · <script type="module"> 부팅
data/state.js         전역 상태 36개 (state 객체 하나)
data/moduleUnit.js    MODULE · SPACES · DEVICES  (단계 2 에서 만든 원본)
data/module.js        공간 계층 · 유닛/타입 · 공용 헬퍼 · 자재 · 배관 · 수명
data/ops.js           알람 · 민원 · 작업 · 권고 · 안전 · 감사 · 에너지
data/const.js         등급 · 상태 코드 · 화면 레지스트리(PAGES/NAVG/RBAC)
render/chart.js       인라인 SVG 차트 헬퍼
render/shell.js       셸 · 라우팅 · 드로어 · AI 패널 · 모달
pages/resident.js     HM-01~HM-13 · RM-01
pages/admin.js        SCR-01~SCR-14 · 협력사 모바일
adapters/dongbaek.js  IoT 실측 주입 어댑터 (아직 미연결 — 단계 2 상태 유지)
data/geometry3d.js    OBJ 에서 자동 생성한 3D 지오메트리 (직접 수정 금지)
render/scene3d.js     3D 실내 뷰 — 라이브러리 없는 SVG 소프트웨어 렌더러
serve.js              로컬 정적 서버 (Node 내장 모듈만)
```

## 3D 실내 뷰

입주민 「우리 집 현황」 중앙 도면에서 **2D 평면 / 3D 실내** 를 전환한다.

| 마우스 | 동작 |
|---|---|
| 좌클릭 | 선택 (공간 · 기기) |
| 휠 | 줌 인 / 아웃 |
| 가운데 버튼 드래그 | 이동 (팬) |
| 우클릭 드래그 | 회전 |

터치 화면은 한 손가락 드래그가 회전이다. 우측 상단 ↺ ↻ ＋ － · **시점 초기화** 버튼도 있다.

- **단면 보기** 를 켜면 벽을 1.25m 에서 잘라 내부가 보인다 (도리집 방식)
- 레이어 : 창호 · 문 · 붙박이장 · 천장 · 냉기 흐름 · 현관 포치
- **선택** : 공간 바닥을 클릭하면 2D 와 똑같이 우측 상세 패널이 열리고,
  기기(TV·에어컨·냉장고·조명)를 클릭하면 제어 다이얼로그가 열린다
- 공간 색·라벨은 상단 지표 탭(에너지·쾌적성·공기질·디바이스)을 따른다

### 지오메트리 재생성

OBJ 가 바뀌면 아래를 다시 실행한다 (`data/geometry3d.js` 가 갱신된다).

```
node build_geo.js "<OBJ 폴더>" data/geometry3d.js
```

## 모듈 평가 순서

`index.html` 이 아래 순서로 import 한다. 순환은 `render/shell.js ↔ pages/*` 한 곳뿐이고,
서로 참조하는 심볼이 모두 함수 선언이라 호이스팅으로 안전하다.
최상위에서 함수를 **호출**하는 코드는 `index.html` 에만 둔다.

```
moduleUnit.js → module.js → ops.js → const.js → state.js → chart.js → shell.js ⇄ pages/*
```
